Fraud Detection API Helm Chart
==============================

This chart deploys the Fraud Detection API.

Install or upgrade:

```sh
helm upgrade --install fraud-detection-api ./infra/k8s/helm/fraud_detection
```

Render locally:

```sh
helm template fraud-detection-api ./infra/k8s/helm/fraud_detection
```
