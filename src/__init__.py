"""Top-level source package for the fraud detection system.

Groups the application code: the prediction service and API
(:mod:`fraud_detection`), the shared database layer (:mod:`database`), the
stream-processing workers (:mod:`workers`) and the scheduled jobs
(:mod:`jobs`).
"""
